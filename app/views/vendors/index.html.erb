<% content_for :javascript_includes do %>
    <%= javascript_include_tag 'jquery.raty.js','letsrate.js'%>
<% end %>

<table>
  <tr>
    <th>Name</th>
    <th>Location</th>
  </tr>

  <% @vendor.each do |vendor| %>
      <tr>
        <td><%= vendor.name %></td>
        <td><%= vendor.location %></td>
        <td>Rating : <%= overall_rating_for_vendor VendorReviewsAndRating, "rating", "vendor_id", vendor.id, :enable_half => true, :half_show => true %></td>
        <td><%= link_to 'View Vendor', vendor_path(vendor) %></td>
      </tr>
  <% end %>
</table>

<p> Number of Hits : <%= @page_hit.count %></p>